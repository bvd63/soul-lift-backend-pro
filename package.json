{
  "name": "soul-lift-backend-zero-errors",
  "version": "1.0.0",
  "description": "Fastify backend (no TypeScript) for SoulLift - Render-ready, zero top-level await issues.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@fastify/cors": "^8.4.2",
    "@fastify/helmet": "^12.1.1",
    "@fastify/rate-limit": "^8.0.0",
    "fastify": "^4.26.2"
  }
}
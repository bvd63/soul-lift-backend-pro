# SoulLift Backend — ZERO-ERROR Render Build

Stable Fastify backend (plain JavaScript, Node 18+) to avoid TS build errors.

## Env
- `OPENAI_API_KEY` = sk-...
- `ORIGIN` = *

## Run
npm i
npm start

## Endpoints
- GET /health
- GET /v1/quote?lang=en&category=motivation
